version: '3'

services:
  products-api:
    build: 
      context: ./
      dockerfile: Dockerfile
      args:
        - PRODUCTS_DATABASE_URL=${PRODUCTS_DATABASE_URL}
    image: southamerica-east1-docker.pkg.dev/somnio-products-api/somnio-products-api/products-api
    environment:
      - PORT=3000
      - DATABASE_URL=${PRODUCTS_DATABASE_URL}
